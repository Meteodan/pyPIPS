# pyPIPS — Claude Code Instructions

Please read `.github/copilot-instructions.md` and follow its instructions.

That file is the single shared instruction source for all AI agents working in
this repository (see also `AGENTS.md` for Codex). Key points it establishes:

- **Read `.github/progress.md` at the start of every session** to pick up recent
  work, lessons learned, and current priorities before making changes.
- **"Update progress"** means append a new session entry to `.github/progress.md`
  in the documented format (most recent first).
- Use **American English** spelling in all generated repository text — code
  comments, docstrings, markdown, commit/PR text, and chat responses about this
  repo.
